name: APPX Publish For Store

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  publish:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Configure git HTTPS
        run: git config --global url."https://github.com/".insteadOf "ssh://git@github.com/"
      - name: Download build zip
        run: gh release download appx-build --pattern "appx-build.zip" --dir .
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: Extract build
        run: Expand-Archive -Path appx-build.zip -DestinationPath electron-app
      - name: Strip mac-only deps + postinstall
        working-directory: electron-app
        run: |
          npm pkg delete devDependencies.dmg-license
          npm pkg delete dependencies.dmg-license
          npm pkg delete optionalDependencies.dmg-license
          npm pkg delete scripts.postinstall
      - name: Install dependencies
        working-directory: electron-app
        run: npm install --legacy-peer-deps --ignore-scripts
      - name: Rebuild native modules win32-x64
        working-directory: electron-app
        run: npx electron-builder install-app-deps --platform=win32 --arch=x64
      - name: Build APPX (Store, unsigned)
        working-directory: electron-app
        run: npx electron-builder@26.0.0 --win appx --x64 --publish never
      - name: Publish APPX as release asset
        working-directory: electron-app
        shell: pwsh
        run: |
          $appx = (Get-ChildItem dist\*.appx | Select-Object -First 1).FullName
          Write-Host "Publishing $appx"
          gh release delete appx-store-1.0.13 --repo $env:GITHUB_REPOSITORY --yes --cleanup-tag 2>$null
          gh release create appx-store-1.0.13 "$appx" --repo $env:GITHUB_REPOSITORY --title "Lyra Music 1.0.13 - APPX for Microsoft Store" --notes "Unsigned APPX for Partner Center upload (Microsoft signs on publish). Built with native modules rebuilt for win32-x64." --prerelease
          Write-Host "DONE"
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
